<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.punbattle.deploy</string>

    <!-- Polls origin every 5 minutes and redeploys when main moves, so a
         `git push` from anywhere is the deploy. Exits silently when nothing
         has changed, and refuses to touch a dirty working tree. -->
    <key>ProgramArguments</key>
    <array>
        <string>/ABSOLUTE/PATH/TO/pun-battle/deploy.sh</string>
        <string>--if-changed</string>
    </array>

    <!-- launchd's default PATH omits Homebrew, and deploy.sh needs git. -->
    <key>EnvironmentVariables</key>
    <dict>
        <key>PATH</key>
        <string>/usr/bin:/bin:/usr/sbin:/sbin:/opt/homebrew/bin:/usr/local/bin</string>
    </dict>

    <key>WorkingDirectory</key>
    <string>/ABSOLUTE/PATH/TO/pun-battle</string>

    <key>StartInterval</key>
    <integer>300</integer>

    <key>RunAtLoad</key>
    <true/>

    <key>StandardOutPath</key>
    <string>/ABSOLUTE/PATH/TO/pun-battle/deploy.log</string>
    <key>StandardErrorPath</key>
    <string>/ABSOLUTE/PATH/TO/pun-battle/deploy.log</string>
</dict>
</plist>
